<h1>sign in!</h1>
<%= form_for(:session, :url => sessions_path) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :password %>
	<%= f.password_field :password %>
  <br/>
  <%= f.submit 'sign in!', :class => 'btn' %>
<% end %>
